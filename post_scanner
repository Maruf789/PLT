{
 let lexbuf = Lexing.from_channel stdin in 
  let msg =
    let cnt =
      let rec count l =
        match token lexbuf with
            EOF -> l
          | _ -> count (l+1)
      in count 0
    in (string_of_int cnt) ^ " tokens"
  in 
  print_endline msg
}
