{
  let lexbuf = Lexing.from_channel stdin in 
  let wordlist =
    let rec next l =
      match token lexbuf with
          EOF -> l
        | _ -> next ("Token " :: l) 
    in next [;]
  in
  List.iter print_newline wordlist
}

