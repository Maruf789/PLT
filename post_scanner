{
  let lexbuf = Lexing.from_channel stdin in 
  let wordlist =
    let rec next l =
      match token lexbuf with
          EOF -> l
        | _ -> next ("Token " :: l) 
    in next ["First";]
  in
  List.iter print_endline wordlist
}

