# function definition
void : hello() def
    disp “hello”;
fed
# top level code 
hello();
int a : 5;
int i : 0;
mat v ;
for i do
    if i <= 5 do
        v : colcat(v, {i});
        i : i + 1;
    else
        break;
    fi
orf
double b : 0.0;
for x in v do
    if x <= 2 then
        b : b + x*x;
    elif x <= 4 then
        b : b + x;
    else
        b : b + x/2.0;
    fi
orf
string endstr : “End” ;
disp endstr ;
